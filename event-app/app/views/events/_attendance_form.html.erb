  <div id="follow_form">
    <% if current_user.attended_events.include?(event) %>
      <%= render 'unattend',:event => event %>
    <% else %>
      <%= render 'attend', :event => event %>
    <% end %>
</div>
